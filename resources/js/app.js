// resources/js/app.js
// Punto de entrada JS principal

import { initComponents } from './modules/components.js';

document.addEventListener('DOMContentLoaded', () => {
    initComponents();
    console.log('App initialized');
});
